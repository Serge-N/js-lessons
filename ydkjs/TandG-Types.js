// types in Js

// null - watch out as it is falsy at times.

// undefined

// boolean

// number

// string

// object

// symbol

// arrays and functions are treated as objects.

console.log(typeof typeof 42);

// undefined is variable is one that has been declared in the accessible scope, but at the moment has no other value in it
// undeclared variable is one that has not been formally declared in the accessible scope.